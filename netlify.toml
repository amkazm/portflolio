[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Official Next.js runtime for Netlify (SSR, API routes, middleware).
[[plugins]]
  package = "@netlify/plugin-nextjs"
